# 🏗️ Lapillo 프로젝트 아키텍처

> 최종 업데이트: 2025-12-10

## 📊 전체 시스템 개요

이 문서는 lapillo 포트폴리오 웹사이트의 전체 서비스 구조와 데이터 흐름을 설명합니다.

```
┌──────────────────────────────────────────────────────────┐
│                    사용자 브라우저                        │
│                 (lapillo.net 접속)                        │
└──────────────────────────────────────────────────────────┘
                            ↓
┌──────────────────────────────────────────────────────────┐
│  ① Cafe24 (도메인 등록소)                                │
│     "이 도메인의 주인은 나입니다"                         │
└──────────────────────────────────────────────────────────┘
                            ↓
┌──────────────────────────────────────────────────────────┐
│  ② Cloudflare (DNS + CDN + 보안)                         │
│     "트래픽을 어디로 보낼지 결정하고 최적화합니다"        │
└──────────────────────────────────────────────────────────┘
                            ↓
┌──────────────────────────────────────────────────────────┐
│  ③ GitHub Pages (웹 호스팅)                              │
│     "HTML, CSS, JavaScript 파일을 제공합니다"            │
└──────────────────────────────────────────────────────────┘
                            ↓ (브라우저에서 JS 실행)
┌──────────────────────────────────────────────────────────┐
│  ④ Firebase (백엔드 데이터베이스 + 스토리지)             │
│     "포트폴리오 데이터와 이미지를 제공합니다"             │
└──────────────────────────────────────────────────────────┘
```

---

## 🔧 사용 중인 서비스

### 1. Cafe24 - 도메인 등록소

**역할**: 도메인 소유권 관리

**등록된 도메인**:
- `lapillo.net` ✅
- `lapillo.co.kr` ✅

**기능**:
- 도메인 구매 및 갱신
- 도메인 소유자 정보 관리
- 네임서버 설정 (Cloudflare로 위임)

**비용**: 도메인 갱신 비용 (연간)

---

### 2. Cloudflare - DNS + CDN + 보안

**역할**: 트래픽 관리 및 최적화

**DNS 관리**:
```
lapillo.net DNS 레코드:
├─ A: @ → 185.199.108.153
├─ A: @ → 185.199.109.153
├─ A: @ → 185.199.110.153
├─ A: @ → 185.199.111.153
└─ CNAME: www → loungebm.github.io
```

**기능**:
- 🟠 CDN (프록시 활성화): 전 세계 캐싱으로 속도 향상
- 🔒 SSL/TLS: 무료 HTTPS 인증서
- 🛡️ DDoS 보호 및 웹 방화벽
- 📊 트래픽 분석

**비용**: 무료 (Free 플랜)

**접속 방법**:
- Cloudflare 대시보드: https://dash.cloudflare.com
- 계정: loungebm@gmail.com

---

### 3. GitHub Pages - 웹 호스팅

**역할**: 정적 웹사이트 호스팅

**리포지토리**: `loungebm/lapillo`

**제공하는 파일**:
```
✅ HTML 파일 (index.html, portfolio-detail.html 등)
✅ CSS 파일 (studio-lapillo.css)
✅ JavaScript 파일 (firebase-cdn.js 등)
✅ 정적 이미지 (로고, 아이콘)
❌ 포트폴리오 데이터 (Firebase Firestore에서)
❌ 업로드된 이미지 (Firebase Storage에서)
```

**자동 배포**:
```yaml
# .github/workflows/deploy.yml
코드 변경 → Git Push → GitHub Actions → 자동 배포
```

**도메인 설정**:
- CNAME 파일: `lapillo.net`
- Settings > Pages > Custom domain

**비용**: 무료 (100GB 대역폭/월)

---

### 4. Firebase - 백엔드 서비스

**역할**: 데이터베이스 + 스토리지 + 인증

**프로젝트 ID**: `lapillo-portfolio`

#### A. Firestore (NoSQL 데이터베이스)

**컬렉션 구조**:
```
lapillo-portfolio
├─ portfolios/
│  ├─ portfolio-001
│  │  ├─ id: string
│  │  ├─ title: string
│  │  ├─ englishTitle: string
│  │  ├─ category: string (design|artwork|exhibition)
│  │  ├─ dateSort: string (YYYY-MM)
│  │  ├─ enabled: boolean
│  │  ├─ textOnly: boolean
│  │  ├─ thumbnail: string (Storage URL)
│  │  └─ images: array<string>
│  └─ ...
│
└─ menus/
   ├─ design {name, order, enabled}
   ├─ artwork {name, order, enabled}
   └─ exhibition {name, order, enabled}
```

#### B. Firebase Storage (이미지 저장소)

**구조**:
```
gs://lapillo-portfolio.firebasestorage.app/
└─ portfolios/
   ├─ {timestamp}_{randomId}_{filename}.jpg
   └─ ...
```

**CORS 설정**: `CORS_SETUP_GUIDE.md` 참고

#### C. Firebase Authentication

**용도**: 관리자 로그인 (`admin-firebase.html`)

**비용**: 무료 (Spark 플랜)
- 제한: 50K 읽기/일, 5GB 저장용량, 1GB 다운로드/일

**접속 방법**:
- Firebase Console: https://console.firebase.google.com
- 프로젝트: lapillo-portfolio

---

## 🌐 데이터 흐름도

### 사용자가 lapillo.net 방문 시

```
1. 브라우저에 "lapillo.net" 입력
   ↓

2. [Cafe24] 
   네임서버 조회 → Cloudflare로 위임
   ↓

3. [Cloudflare DNS]
   lapillo.net → 185.199.108.153 (GitHub Pages)
   ↓

4. [Cloudflare CDN]
   - 캐시 확인
   - 없으면 GitHub Pages에서 가져옴
   - 사용자에게 제공 (빠름!)
   ↓

5. [GitHub Pages]
   index.html 파일 제공
   ↓

6. 브라우저가 HTML 렌더링
   - CSS 로드 → 스타일 적용
   - JavaScript 로드
   - firebase-cdn.js 실행
   ↓

7. [Firebase Firestore]
   JavaScript가 API 호출:
   firebaseService.getAllPortfolios()
   ↓
   응답: 포트폴리오 데이터 배열
   ↓

8. [Firebase Storage]
   이미지 URL 요청
   ↓
   이미지 다운로드
   ↓

9. 브라우저에서 최종 렌더링
   ✅ 완성된 포트폴리오 표시!
```

---

## 📊 서비스별 역할 요약

| 서비스 | 주요 역할 | 제공하는 것 | 비용 | 접속 방법 |
|--------|----------|------------|------|----------|
| **Cafe24** | 도메인 소유권 | 도메인 등록 | 연간 갱신비 | cafe24.com |
| **Cloudflare** | DNS + CDN + 보안 | 빠른 속도, HTTPS | 무료 | dash.cloudflare.com |
| **GitHub Pages** | 웹 호스팅 | HTML, CSS, JS | 무료 | github.com/loungebm/lapillo |
| **Firebase** | 백엔드 데이터 | 데이터 + 이미지 | 무료 | console.firebase.google.com |

---

## 🔄 배포 프로세스

```
1. 코드 수정 (로컬)
   ↓

2. Git Commit & Push
   $ git add .
   $ git commit -m "Update feature"
   $ git push origin main
   ↓

3. GitHub Actions 자동 실행
   (.github/workflows/deploy.yml)
   ↓

4. GitHub Pages 자동 배포
   ↓

5. Cloudflare CDN 캐시 업데이트
   (자동 또는 수동 퍼지)
   ↓

6. 배포 완료!
   https://lapillo.net
```

---

## 🌍 도메인 설정

### 현재 상태

```
lapillo.net: ✅ 활성화
├─ Cafe24: 도메인 등록
├─ Cloudflare: DNS 관리
├─ GitHub Pages: CNAME 설정
└─ 상태: 정상 작동

lapillo.co.kr: ⏳ 설정 예정
└─ 추가 설정 필요
```

### lapillo.co.kr 추가 계획

> **TODO**: lapillo.co.kr 설정
> - [ ] Cloudflare에 도메인 추가
> - [ ] DNS 레코드 복사 (lapillo.net과 동일)
> - [ ] Cafe24에서 네임서버 변경
> - [ ] GitHub Pages 설정 업데이트
> - [ ] 또는 Vercel 마이그레이션 고려

---

## 💡 장점 및 제약사항

### ✅ 장점
- **완전 무료** (도메인 제외)
- **빠른 속도** (Cloudflare CDN)
- **안전함** (Cloudflare 보안, Firebase 규칙)
- **관리 쉬움** (자동 배포, GUI 관리)
- **확장 가능** (서버리스 아키텍처)

### ⚠️ 제약사항
- **GitHub Pages**: 1개 커스텀 도메인만 공식 지원
- **Firebase 무료 플랜**: 일일 읽기/쓰기 제한 존재
- **정적 사이트**: 서버사이드 로직 불가능 (클라이언트 사이드만)

---

## 🔒 보안 설정

### Firebase Security Rules

**Firestore** (`firestore.rules`):
- 읽기: 공개 (enabled=true인 항목만)
- 쓰기: 인증된 관리자만

**Storage** (`storage.rules`):
- 읽기: 공개
- 쓰기: 인증된 관리자만

**Authentication**:
- 관리자 계정으로 제한
- Custom claims 사용

---

## 📞 문제 해결

### 포트폴리오가 로드되지 않는 경우
1. Firebase 설정 확인 (`js/firebase-cdn.js`)
2. Firestore 보안 규칙 확인
3. 브라우저 콘솔 에러 확인
4. 네트워크 탭에서 API 호출 확인

### 이미지가 로드되지 않는 경우
1. Firebase Storage CORS 설정 확인 (`CORS_SETUP_GUIDE.md`)
2. 이미지 URL 확인 (Firebase Console)
3. Storage 보안 규칙 확인

### DNS 문제
1. Cloudflare DNS 레코드 확인
2. DNS 전파 상태 확인 (dnschecker.org)
3. 캐시 문제일 경우 Cloudflare에서 캐시 퍼지

---

## 📚 관련 문서

- [README.md](./README.md) - 프로젝트 소개 및 사용법
- [FIREBASE_SETUP.md](./FIREBASE_SETUP.md) - Firebase 설정 가이드
- [CORS_SETUP_GUIDE.md](./CORS_SETUP_GUIDE.md) - CORS 설정 가이드

---

## 📝 변경 이력

- **2025-12-10**: 초기 아키텍처 문서 작성
  - Cafe24, Cloudflare, GitHub Pages, Firebase 구조 정리
  - 데이터 흐름 및 서비스별 역할 명시
  
<!-- 향후 업데이트
- **YYYY-MM-DD**: lapillo.co.kr 도메인 추가
- **YYYY-MM-DD**: Vercel 마이그레이션 (선택사항)
-->

---

© 2025 Lapillo Design Studio. All rights reserved.

