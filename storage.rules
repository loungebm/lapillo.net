rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // 모든 읽기/쓰기 허용 (개발용)
    allow read, write: if true;
    
    // 포트폴리오 폴더에 대한 명시적 규칙
    match /portfolios/{allPaths=**} {
      allow read, write: if true;
    }
  }
}
